<UserControl x:Class="PersonGrid.Views.PersonGridView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PersonGrid.Views"
        mc:Ignorable="d"
        Height="550" Width="900">
    <Grid>
        <DataGrid ItemsSource="{Binding Persons, Mode=OneWay}" AutoGenerateColumns="False" >
            <DataGrid.Columns>
                <!-- <DataGridTextColumn Header="User Id" Binding="{Binding Guid}"></DataGridTextColumn> -->
                <!-- <DataGridTextColumn Header="Login" Binding="{Binding Login}"></DataGridTextColumn> -->
                <DataGridTextColumn Header="First name"  Binding="{Binding FirstName, Delay=5 }"></DataGridTextColumn>
                <DataGridTextColumn Header="Last name" Binding="{Binding LastName, Delay=5}"></DataGridTextColumn>
                <DataGridTextColumn Header="E-mail" Binding="{Binding Email, Delay=5}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
</Grid>

</UserControl>
